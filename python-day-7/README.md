# Day 7 - Python Lists

## Topics Covered
- Introduction to Lists
- Creating a List
- Accessing List Elements
- Adding Elements using `append()`
- Removing Elements using `remove()`
- Finding List Length using `len()`
- Using `for` loop with Lists

## Practice Programs
- Print a list of movies
- Print first and last elements
- Add a new element to a list
- Remove an element from a list
- Find the length of a list

## Mini Task
Student Marks Management
- Displayed student marks
- Calculated total using `sum()`
- Found highest mark using `max()`
- Found lowest mark using `min()`
- Calculated average using `sum()` and `len()`

## Outcome
Learned how to create and manipulate Python lists using built-in functions and basic list operations.
