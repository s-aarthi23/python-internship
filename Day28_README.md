# 🐍 Day 28 - Lambda, Map & Filter

Today I learned how to work with Lambda Functions and use them with `map()` and `filter()` in Python.

## 📚 Topics Covered

- Lambda Functions
- `map()`
- `filter()`
- Lambda with `map()`
- Lambda with `filter()`

## 🧠 What I Learned

### Lambda Function
Lambda is a small one-line function used for simple operations.

### map()
`map()` is used to apply a function to every item in a list.

### filter()
`filter()` is used to select items that satisfy a particular condition.

## 💻 Practice

I practiced:

- Finding the square of a number
- Finding the larger of two numbers
- Doubling numbers using `map()`
- Finding even numbers using `filter()`
- Finding numbers greater than 10

## 🚀 Mini Project - Number Analyzer

I created a simple Number Analyzer using:

- Lambda
- `map()`
- `filter()`

The program analyzes a list of numbers and displays:

- Original numbers
- Doubled numbers
- Even numbers
- Numbers greater than 10

### Example

```text
Original: [5, 12, 8, 20, 3, 15]
Doubled: [10, 24, 16, 40, 6, 30]
Even Numbers: [12, 8, 20]
Greater than 10: [12, 20, 15]

💡 Key Takeaway

Today I learned how Python's Lambda Functions can make simple operations shorter and how map() and filter() can be used to process lists easily.

📈 Progress

Day 28/100 ✅

🐍 Continuing my Python learning journey!

