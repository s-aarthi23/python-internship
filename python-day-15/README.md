# Day 15 – Polymorphism

## 📚 Topics Covered
- What is Polymorphism?
- Method Overriding
- Same method with different behaviour
- Polymorphism with loops

## 💡 Practice
- Created `Dog` and `Cat` classes with the same `sound()` method.
- Created `Car` and `Boat` classes with the same `move()` method.
- Used a loop with `Dog`, `Cat`, and `Cow` objects to demonstrate polymorphism.

## 🎯 Mini Task
Created a simple Payment System using:
- UPI
- Card
- Cash

Each class has the same `pay()` method with different outputs.

## 🛠️ Skills Practiced
- Classes and Objects
- Method Overriding
- Polymorphism
- Lists
- For Loops

## ✅ Status
Day 15 Completed 🎉
